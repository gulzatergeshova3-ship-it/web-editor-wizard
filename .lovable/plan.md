В hero-секции (`src/routes/index.tsx`) под карточкой countdown видно ~2 ряда пустых клеток фонового грида (48×48px). Это из-за симметричного вертикального паддинга `py-16 lg:py-24`.

**Изменение:** заменить в `<section>` hero классы `py-16 lg:py-24` на `pt-16 lg:pt-24 pb-4 lg:pb-6`, чтобы убрать лишние ~96px снизу, сохранив верхний отступ от хедера.

Больше ничего не трогаю.